package com.example.ciapplication

class UtilskitTest {
/*
    @Test
    fun testValidPassword() {
        val password = "Passw0rd!"

        // Action
        val results = verifyPassword(password)

        // Result
        assertTrue(results.isEmpty())
    }

    @Test
    fun testlessThanSixCharPassword() {
        val password = "Pwd!"

        // Action
        val results = verifyPassword(password)

        // Result
        assertEquals(results.size, 1)
        assertTrue(results.isEmpty())
    }
    */

}